import { IConfiguration } from '../helpers/project-config.js';
export interface IProjectConfigCheckParams {
    checkName?: boolean;
    checkHandle?: boolean;
    checkVersion?: boolean;
    checkType?: boolean;
    checkEntrypoint?: boolean;
    checkPluginParams?: boolean;
    willDeploy?: boolean;
    configTemplate?: Record<string, Record<string, any>>;
}
export declare function getProjectConfigWizard(params?: IProjectConfigCheckParams): Promise<IConfiguration>;
