import { CliUx } from '@oclif/core';
export async function getProjectName(params) {
    if (params.provided) {
        return params.provided;
    }
    return await CliUx.ux.prompt(params.prompt || 'Project name?', { default: params.suggested });
}
