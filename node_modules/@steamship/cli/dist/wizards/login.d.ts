import { Configuration } from '@steamship/client';
export interface ILoginParams {
    config: Configuration;
    api_domain?: string;
    site_domain?: string;
}
export declare function isLoggedIn(config: Configuration): boolean;
export declare function doLogin(params: ILoginParams): Promise<void>;
