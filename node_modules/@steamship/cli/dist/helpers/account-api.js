import got from 'got';
import { getAuthenticatedUrl } from './api-utils.js';
export async function getAccount() {
    const { url, headers } = await getAuthenticatedUrl();
    const { data } = (await got.get(url, { headers: headers }).json());
    return data.user;
}
