#!/usr/bin/env NODE_OPTIONS='--trace-warnings,--experimental-vm-modules,--no-warnings,--no-experimental-fetch' ts-node
import {Errors, flush, run} from '@oclif/core';

run(void 0, import.meta.url)
    .then(flush)
    .catch((error) => {
        return Errors.handle(error)
    })
