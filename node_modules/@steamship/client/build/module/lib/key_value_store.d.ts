import { File } from './file.js';
import { IApiBase } from './shared/BaseInterfaces.js';
import { Tag } from './tag.js';
import { Task } from './task.js';
export type DICT = {
    [key: string]: string | boolean | number | DICT;
};
export declare class KeyValueStore {
    client: IApiBase;
    storeIdentifier: string;
    constructor(client: IApiBase, storeIdentifier?: string);
    _getFile(orCreate?: boolean): Promise<File | undefined>;
    get(key: string): Promise<DICT | undefined>;
    delete(key: string): Promise<boolean>;
    set(key: string, value: DICT): Promise<Task<Tag>>;
    items(filterKeys?: string[]): Promise<[string, DICT][]>;
    reset(): Promise<void>;
}
